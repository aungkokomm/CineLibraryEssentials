<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CineLibraryEssentials.Views.ScrapingStep"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid RowDefinitions="Auto,*,Auto" RowSpacing="8">

        <!-- Toolbar -->
        <Grid Grid.Row="0" ColumnDefinitions="Auto,*" ColumnSpacing="8">
            <Button x:Name="AddFolderButton"
                    Click="OnAddFolderClick"
                    ToolTipService.ToolTip="Add another movie folder">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE710;" FontSize="13" />
                    <TextBlock Text="Add Folder" FontSize="13" />
                </StackPanel>
            </Button>
            <TextBlock Grid.Column="1"
                       x:Name="StatusText"
                       Text="{Binding StatusMessage}"
                       VerticalAlignment="Center"
                       FontSize="11"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       TextTrimming="CharacterEllipsis" />
        </Grid>

        <!-- Movie list -->
        <ListView Grid.Row="1"
                  x:Name="MovieList"
                  SelectionMode="None"
                  BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                  BorderThickness="1"
                  CornerRadius="4">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="36,*,140,Auto"
                          Padding="10,8"
                          Background="{ThemeResource LayerFillColorDefaultBrush}"
                          BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                          BorderThickness="0,0,0,1">
                        <TextBlock Text="" />
                        <TextBlock Grid.Column="1" Text="Movie"
                                   FontWeight="SemiBold" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock Grid.Column="2" Text="Status"
                                   FontWeight="SemiBold" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.Header>
                <Grid />
            </ListView.Header>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="10,4" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="36,*,140,Auto">
                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                  VerticalAlignment="Center" />

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="1">
                            <TextBlock Text="{Binding DisplayName}"
                                       FontWeight="SemiBold"
                                       FontSize="13"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding FolderPath}"
                                       FontSize="10"
                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTipService.ToolTip="{Binding FolderPath}" />
                        </StackPanel>

                        <TextBlock Grid.Column="2"
                                   Text="{Binding Status}"
                                   VerticalAlignment="Center"
                                   FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                        <StackPanel Grid.Column="3"
                                    Orientation="Horizontal"
                                    Spacing="2"
                                    VerticalAlignment="Center">
                            <Button Tag="{Binding}"
                                    Click="OnScrapeRowClick"
                                    Padding="8,2"
                                    ToolTipService.ToolTip="Search TMDb and scrape this movie">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <FontIcon Glyph="&#xE721;" FontSize="13" />
                                    <TextBlock Text="Scrape" FontSize="12" />
                                </StackPanel>
                            </Button>
                            <Button Tag="{Binding}"
                                    Click="OnOpenRowClick"
                                    Padding="8,2"
                                    ToolTipService.ToolTip="Open folder in Explorer">
                                <FontIcon Glyph="&#xE838;" FontSize="13" />
                            </Button>
                            <Button Tag="{Binding}"
                                    Click="OnRemoveRowClick"
                                    Padding="8,2"
                                    ToolTipService.ToolTip="Remove from list">
                                <FontIcon Glyph="&#xE74D;" FontSize="13" />
                            </Button>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Footer -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="8"
                    Margin="0,4,0,0">
            <Button x:Name="ScrapeSelectedButton"
                    Content="Scrape Selected"
                    Click="OnScrapeSelectedClick" />
            <Button x:Name="SaveButton"
                    Content="Save"
                    Style="{ThemeResource AccentButtonStyle}"
                    Click="OnSaveClick" />
        </StackPanel>
    </Grid>
</UserControl>
